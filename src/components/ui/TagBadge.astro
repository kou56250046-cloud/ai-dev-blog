---
interface Props {
  tag:  string;
  href?: string;
}

const { tag, href = `/tags/${tag}` } = Astro.props;
---

<a href={href} class="tag-badge hover:bg-accent/15 transition-colors">
  #{tag}
</a>
